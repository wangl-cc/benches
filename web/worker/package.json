{
  "name": "benchmark-explorer-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=26 <27",
    "pnpm": ">=10 <12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260520.0",
    "wrangler": "^4.93.1"
  },
  "scripts": {
    "dev": "wrangler dev --config ../wrangler.jsonc",
    "deploy": "wrangler deploy --config ../wrangler.jsonc",
    "migrate": "wrangler d1 migrations apply BENCH_DB --config ../wrangler.jsonc"
  }
}
